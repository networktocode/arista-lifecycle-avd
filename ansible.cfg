[defaults]
# AVD recommended values
duplicate_dict_key=error
forks = 25
# global_vars plugin reads sites/_global_vars/ next to each site inventory
vars_plugins_enabled = arista.avd.global_vars, host_group_vars
roles_path = ./roles

[vars_global_vars]
paths = ../_global_vars/
